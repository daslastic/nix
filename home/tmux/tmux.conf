# tmux-nvim 
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?\.?(view|n?vim?x?)(-wrapped)?(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h' { if -F '#{pane_at_left}' \'\' 'select-pane -L' }
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j' { if -F '#{pane_at_bottom}' \'\' 'select-pane -D' }
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k' { if -F '#{pane_at_top}' \'\' 'select-pane -U' }
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l' { if -F '#{pane_at_right}' \'\' 'select-pane -R' }
bind-key -T copy-mode-vi 'C-h' if -F '#{pane_at_left}' '\' 'select-pane -L'
bind-key -T copy-mode-vi 'C-j' if -F '#{pane_at_bottom}' '\' 'select-pane -D'
bind-key -T copy-mode-vi 'C-k' if -F '#{pane_at_top}' '\' 'select-pane -U'
bind-key -T copy-mode-vi 'C-l' if -F '#{pane_at_right}' '\' 'select-pane -R'

is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind -n 'M-h' if-shell "$is_vim" 'send-keys M-h' 'resize-pane -L 1'
bind -n 'M-j' if-shell "$is_vim" 'send-keys M-j' 'resize-pane -D 1'
bind -n 'M-k' if-shell "$is_vim" 'send-keys M-k' 'resize-pane -U 1'
bind -n 'M-l' if-shell "$is_vim" 'send-keys M-l' 'resize-pane -R 1'

bind-key -T copy-mode-vi M-h resize-pane -L 1
bind-key -T copy-mode-vi M-j resize-pane -D 1
bind-key -T copy-mode-vi M-k resize-pane -U 1
bind-key -T copy-mode-vi M-l resize-pane -R 1

# other
set-option -g renumber-windows on
set-option -a terminal-overrides ",xterm-256color:Tc"

set-option -g update-environment "COLOR_WHITE COLOR_GRAY COLOR_BLACK COLOR_LIGHT_PURPLE COLOR_DARK_PURPLE COLOR_CYAN COLOR_GREEN COLOR_ORANGE COLOR_RED COLOR_PINK COLOR_YELLOW"
 
# gruvbox
set-option -g pane-active-border-style "fg=${COLOR_LIGHT_PURPLE}"
set-option -g pane-border-style "fg=${COLOR_GRAY}"
set-option -g status-style "bg=${COLOR_BLACK},fg=${COLOR_WHITE}"
set-option -g status-right "#[bg=${COLOR_GREEN},fg=${COLOR_WHITE}]#{?client_prefix,#[bg=${COLOR_YELLOW}],} #S #[bg=${COLOR_CYAN},fg=${COLOR_WHITE}] #H "
set-option -g status-left ""
set-window-option -g window-status-current-format "#[fg=${COLOR_WHITE},bg=${COLOR_DARK_PURPLE}] #I #W "
set-window-option -g window-status-format "#[fg=${COLOR_WHITE}]#[bg=${COLOR_BLACK}] #I #W "
